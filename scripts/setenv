dirs=(
	"/mnt/xilinx"
	"/opt/Xilinx"
)

VIVADO_BIN="Vivado/2016.3/bin/vivado"
HSI_BIN="SDK/2016.3/bin/hsi"
for item in ${dirs[@]};do
	if [ -d "$item" ]; then
		VIVADO_BIN="${item}/${VIVADO_BIN}"
		HSI_BIN="${item}/${HSI_BIN}"
		echo "the vivado binary is located in ${VIVADO_BIN}"
		break
	fi
done
